<% @share_url = "#{ENV['PROD_SITE_URL']}#{request.env['PATH_INFO']}" %>
<table>
	<tr>
		<td>
			<%= @share_url %>
			<%= twitter_button :url=> "", :text=> "I just brewed a beer! Check it out at Fermently!", :via=> "", :hashtags=> "Fermently" %>
		</td>
		<td>
			<%= facebook_button :send=> 'true', :url=> @share_url, layout: "button" %>
		</td>
	</tr>
</table>